# react-hrnet-modal

A lightweight, controlled React modal component inspired by the UI behavior of `jquery-modal`.
It provides overlay click to close, Escape key handling, optional close button, and scroll locking.

## Installation

### npm
```bash
npm install react-hrnet-modal